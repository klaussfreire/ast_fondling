These are examples for how to manipulate the python AST to do some basic code transformations

You can test some:

.. code::

    python rewrite.py example.py constprop inline constprop inline constprop js2

